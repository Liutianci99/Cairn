# 接需求开场

老项目接到新需求时的固定开场，按顺序执行：

1. 切到目标仓库目录
2. `git pull` 拉取最新代码
3. `read` 仓库全部文件，了解项目结构与当前进展
4. 把需求逐条编号，复述确认后再动手

> 反例：跳过 read 直接改代码 → 与既有实现冲突、重复造轮子。

立项流程、迭代循环、收尾与铁律见仓库根目录 `CLAUDE.md`。每个需求的提交调用 `commit` 技能，新建 PRD 调用 `prd` 技能。
